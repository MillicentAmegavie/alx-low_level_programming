#include "main.h"

/**
 * _abs - The function that computes the absolute value of an integer.
 * @n: Integer to be computed.
 *
 * Return: n or -n.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
