#include <stdio.h>

/**
 * main - The Entry Point
 *
 * Print all single digit numbers of base 10 starting from 0.
 *
 * Return: 0 on Success.
 */

int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
		printf("%d", m);
	printf("\n");
	return (0);
}
