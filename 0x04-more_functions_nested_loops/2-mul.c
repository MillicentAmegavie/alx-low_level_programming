#include "main.h"

/**
 * mul - The function that multiplies two integers (a, b).
 * @a: The first parameter.
 * @b: The second parameter.
 *
 * Return: a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
